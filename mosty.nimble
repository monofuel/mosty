version     = "0.1.0"
author      = "monofuel"
description = "Mattermost API client library for Nim"
license     = "MIT"

srcDir = "src"

requires "nim >= 2.0.0"
requires "curly >= 1.1.1"
requires "jsony >= 1.1.5"
requires "ws >= 0.5.0"
requires "webby >= 0.2.1"
